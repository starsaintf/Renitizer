FROM node:22-bookworm-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends ffmpeg \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY filter.mjs server.mjs ./

ENV PORT=8080
EXPOSE 8080
CMD ["node", "server.mjs"]

// Copy to config.js only if you want a default endpoint in a custom deployment.
// This file must never contain a provider key. Keys live in the Worker secret store.
export const RENITIZER_CONFIG = { cloudEndpoint: 'https://your-worker.example/api/analyze' };

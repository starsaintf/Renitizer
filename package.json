{
  "name": "renitizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/main.js",
    "build:native-web": "node scripts/build-native-web.mjs",
    "native:sync": "npm run build:native-web && cap sync",
    "native:android": "cap open android",
    "native:ios": "cap open ios",
    "desktop:build": "npm run build:native-web && tauri build --config src-tauri/tauri.conf.json"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@tauri-apps/cli": "^2.11.4"
  },
  "overrides": {
    "xcode": {
      "uuid": "11.1.1"
    }
  }
}
